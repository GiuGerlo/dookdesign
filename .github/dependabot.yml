version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # Un solo PR semanal con todos los patch/minor para no inundar.
      all-minor-patch:
        update-types:
          - minor
          - patch
